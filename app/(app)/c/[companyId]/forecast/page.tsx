import { ModulePlaceholder } from "@/components/shell/ModulePlaceholder";

export default function ForecastPage() {
  return (
    <ModulePlaceholder
      title="Forecast & Budget"
      subtitle="Manage forecast versions and budget grids (mirrors cash flow structure)"
      phase="Phase 4"
    />
  );
}
