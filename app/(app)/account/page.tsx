import { ModulePlaceholder } from "@/components/shell/ModulePlaceholder";

export default function AccountPage() {
  return (
    <ModulePlaceholder
      title="Account Settings"
      subtitle="Personal settings - distinct from the Admin Console"
      phase="Phase 8"
      usesPeriod={false}
    />
  );
}
